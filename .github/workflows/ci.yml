name: CI with Cache

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: prime-cache-4d7e879
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-4d7e879

      - name: Echo cache result
        run: echo "Cache hit result recorded"
